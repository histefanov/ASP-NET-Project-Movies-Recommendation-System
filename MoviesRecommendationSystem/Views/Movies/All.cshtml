@model AllMoviesQueryModel

@{
    ViewData["Title"] = "Collection";
}

<form method="get">
    <div class="row" mb-5>
        <div class="form-group col-md-4">
            <select asp-for="SelectedGenre" class="form-control">
                <option value="">All Genres</option>
                @foreach (var genre in Model.Genres)
                {
                    <option value="@genre">@genre</option>
                }
            </select>
        </div>
        <div class="form-group col-md-4">
            <select asp-for="Sorting" class="form-control">
                <option value="0" selected>Date of addition</option>
                <option value="1">Year of Release (desc.)</option>
                <option value="2">Year of Release (asc.)</option>
            </select>
        </div>
        <div class="input-group col-md-4">
            <input asp-for="SearchTerm" class="form-control">
        </div>
    </div>
</form>
<div class="row">
    @foreach (var movie in Model.Movies)
    {
        <div class="card">
            <a href="#">
                <img class="card-img-top" src="@movie.ImageUrl" alt="@movie.Title (@movie.ReleaseYear)">
            </a>
            <div class="card-body">
                <a href="#">
                    <h5 class="card-title">@movie.Title, @movie.ReleaseYear</h5>
                </a>
                <p class="card-text">@movie.Plot</p>
            </div>
        </div>
    }
</div>

@{
    var previousPage = Model.CurrentPage - 1;
    var nextPage = Model.CurrentPage + 1;

    if (previousPage < 1)
    {
        previousPage = 1;
    }

    bool hasNextPage = Model.CurrentPage * AllMoviesQueryModel.MoviesPerPage < Model.TotalMoviesCount;
}

<div class="row">
    @if (Model.CurrentPage > 1)
    {
        <div class="pagination-button">
            <a class="pagination" 
               asp-controller="Movies" 
               asp-action="All" 
               asp-route-currentPage="@previousPage"
               asp-route-selectedGenre="@Model.SelectedGenre"
               asp-route-sorting="@((int)Model.Sorting)"
               asp-route-searchTerm="@Model.SearchTerm">
                Previous
            </a>
        </div>
    }
    @if (hasNextPage)
    {
        <div class="pagination-button">
            <a class="pagination" 
               asp-controller="Movies" 
               asp-action="All" 
               asp-route-currentPage="@nextPage"
               asp-route-selectedGenre="@Model.SelectedGenre"
               asp-route-sorting="@((int)Model.Sorting)"
               asp-route-searchTerm="@Model.SearchTerm">
                Next
            </a>
        </div>
    }
</div>


